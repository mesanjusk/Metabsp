// Placeholder root page. The dashboard UI port (frontend/src/Pages,
// frontend/src/Components/whatsappCloud/*) has not started yet — see
// nextjs/STATUS.md. This app currently only serves the API routes under
// app/api/ and app/webhook/.
export default function Home() {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem' }}>
      <h1>Metabsp</h1>
      <p>Next.js 15 API slice — dashboard UI not yet ported. See nextjs/STATUS.md.</p>
    </main>
  );
}
